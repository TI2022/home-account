#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged first
npx lint-staged

# Run tests if not in CI environment
if [ -z "$CI" ]; then
  echo "🧪 Running unit tests before commit (Playwright not required)..."
  npm run test:run:unit
fi
